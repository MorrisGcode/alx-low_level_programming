#include "main.h"
#include <stdio.h>
#include <math.h>
/**
  *_sqrt_recursion - returns natural sqrt of a number
  *@n: integer
  *Return: sqroot(integer)
  */

int _sqrt_recursion(int n)
{
	if (n == 0)
		return (-1);

	return (_sqrt_recursion(return sqrt(n)));
}

